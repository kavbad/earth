/**
 * @earth/server — Pure server-tier handlers (token, webhooks, push, verification, feed, sweeps) with injected deps.
 *
 * See docs/architecture/ARCHITECTURE.md §2/§3. Later changes add the real modules here;
 * keep this file as the single public entry point of the package.
 */
export const PACKAGE_NAME = '@earth/server' as const
