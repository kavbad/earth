/**
 * @earth/api — EarthClient: typed wrapper over supabase-js RPC/select/realtime/storage and fetch to the server tier.
 *
 * See docs/architecture/ARCHITECTURE.md §2/§3. Later changes add the real modules here;
 * keep this file as the single public entry point of the package.
 */
export const PACKAGE_NAME = '@earth/api' as const
