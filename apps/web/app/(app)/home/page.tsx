import { HomeFeed } from '../../../components/feed/HomeFeed'

/**
 * SCREEN 01–05 — Home: the public World for Visitors, Friends / Neighborhood / City / World for
 * Humans. Everything renders on the client against the person's session and radius.
 */
export default function HomePage() {
  return <HomeFeed />
}
