import { NotificationsList } from '@/components/feed/notifications/NotificationsList'

/** `/notifications` — SCREEN 23 (also where a social push lands). */
export default function NotificationsRoute() {
  return <NotificationsList />
}
