/** `/you/settings/safety` — SCREEN 25 → Safety. */
import { SafetySettingsScreen } from '@/features/earth/screens/SafetySettingsScreen'

export default function SafetySettingsRoute() {
  return <SafetySettingsScreen />
}
