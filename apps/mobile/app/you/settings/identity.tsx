/** `/you/settings/identity` — SCREEN 25 → Human identity. */
import { IdentitySettingsScreen } from '@/features/earth/screens/IdentitySettingsScreen'

export default function IdentitySettingsRoute() {
  return <IdentitySettingsScreen />
}
