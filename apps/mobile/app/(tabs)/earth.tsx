/** SCREEN 20 — Earth: the radius and the map (`components/map/EarthScreen`). */
import { EarthScreen } from '@/components/map/EarthScreen'

export default function EarthTab() {
  return <EarthScreen />
}
