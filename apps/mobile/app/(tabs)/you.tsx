/** SCREEN 24 — You: own profile, posts, quiet counts, Settings and "Your Earth". */
import { YouScreen } from '@/features/earth/screens/YouScreen'

export default function YouTab() {
  return <YouScreen />
}
