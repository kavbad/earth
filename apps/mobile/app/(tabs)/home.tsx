import { HomeFeed } from '@/components/feed/HomeFeed'

/** SCREEN 01–05 — Home: the public World for Visitors, the person's radius for Humans. */
export default function HomeTab() {
  return <HomeFeed />
}
